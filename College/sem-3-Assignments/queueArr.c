// gcc queueArr.c -o a
#include <stdio.h>
#include <stdlib.h>
int Queue[100];
int front = -1, rear = -1;

void main()
{
    int size;
    printf("Enter the size of the queue here: ");
    scanf("%d",&size);

}
//postponed